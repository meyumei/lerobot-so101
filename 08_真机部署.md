# 真机部署

## 1. 当前状态

模型已经下载到：

```text
F:\260826_so101\models\act_pick_blue_box_export
```

重点首先测试：

```text
008000
```

当前 8k 模型目录中已有：

```text
config.json
model.safetensors
policy_preprocessor.json
policy_preprocessor_step_3_normalizer_processor.safetensors
policy_postprocessor.json
policy_postprocessor_step_0_unnormalizer_processor.safetensors
train_config.json
```

---

## 2. 回退备份环境（0.6.1，不用于当前部署）

升级前的历史环境已保留为回退备份：

```text
环境路径  F:\miniconda3\envs\lerobot-so101-clean-backup
Python    3.12.14
LeRobot   0.6.1
```

`lerobot-rollout` 已存在。

但当前 ACT checkpoint 是用：

```text
LeRobot 0.6.2
commit 4aaff99be4a1d81568c08c8f0296b41b40c99ec4
```

训练得到。

该环境只用于追溯和回退，不作为当前部署环境。

---

## 3. 当前部署环境（已完成升级）

不再另外创建 `lerobot-so101-deploy`。当前直接使用已经升级并验证依赖完整的环境：

```text
环境名       lerobot-so101-clean
环境路径     F:\miniconda3\envs\lerobot-so101-clean
Python       3.12.14
LeRobot      0.6.2
Commit       4aaff99be4a1d81568c08c8f0296b41b40c99ec4
PyTorch      2.10.0+cu126
TorchVision  0.25.0+cu126
TorchCodec   0.10.0
PyAV         15.1.0
FFmpeg       7.1.1
```

本地驱动 555.97 暂不满足 cu128 完全对齐条件，所以当前保留 CUDA 可用的 cu126。首次部署前先做模型只读加载验证；未经确认不升级驱动或替换为 cu128。完整说明见 [00_项目总览.md](./00_项目总览.md)。

---

## 4. 检查本地模型配置

```cmd
python -c "from lerobot.policies.act.configuration_act import ACTConfig; c=ACTConfig.from_pretrained(r'F:\260826_so101\models\act_pick_blue_box_export\008000'); print(c)"
```

---

## 5. 真机部署前必须确认

```text
Follower COM       = ?
robot.id           = ?
overhead index     = ?
right_wrist index  = ?
CUDA available     = ?
```

检查端口：

```cmd
lerobot-find-port
```

检查相机：

```cmd
lerobot-find-cameras
```

---

## 6. 相机输入要求

当前 ACT 模型训练时使用：

```text
observation.images.overhead
observation.images.right_wrist
```

所以部署时必须提供：

```text
overhead
right_wrist
```

两个 key。

---

## 7. 首次 rollout 安全原则

首次真机自主控制：

- 先使用 8k checkpoint。
- 时间只设 5～10 秒。
- 蓝色方块放在训练时常见位置。
- 保持采集时相机位置。
- 机械臂周围清空障碍物。
- 人保持在电源/急停附近。
- 出现异常运动立即停止。
- 注意 `lerobot-rollout` 可能默认在结束后将机械臂插值返回启动姿态。

---

## 8. 最终 rollout 命令

当前还缺少实际的：

```text
Follower COM
robot.id
overhead camera index
right_wrist camera index
本机 CUDA 状态
```

因此暂时不写死最终 rollout 命令。

确认这些参数以后再补充。
