# 模型导出与备份

> 当前基准：ACT v2，83 episodes，30,000 steps。  
> 本文以当前第二轮训练为例，同时作为以后每轮训练完成后的标准导出流程。

## 1. 当前第二轮训练产物

本轮训练：

```text
Run name        act_pick_blue_box_v2_83ep
Episodes        83
Frames          34248
Steps           30000
Approx epochs   7.01
Save freq       5000
ACT chunk_size  100
n_action_steps  100
```

当前真机优先比较：

```text
020000
025000
030000
```

## 2. 找到实际 checkpoint 目录

训练时如果 `--output_dir` 使用相对路径，保存位置取决于启动训练时的当前目录。

先查找：

```bash
find ~ -type d -path "*/act_pick_blue_box_v2_83ep/checkpoints" -print 2>/dev/null
```

当前第二轮实际训练目录：

```text
/home/featurize/outputs/train/act_pick_blue_box_v2_83ep
```

检查：

```bash
ls -lah ~/outputs/train/act_pick_blue_box_v2_83ep/checkpoints
ls -lah ~/outputs/train/act_pick_blue_box_v2_83ep/checkpoints/030000/pretrained_model
```

每个 `pretrained_model` 应至少包含：

```text
config.json
model.safetensors
policy_preprocessor.json
policy_preprocessor_step_3_normalizer_processor.safetensors
policy_postprocessor.json
policy_postprocessor_step_0_unnormalizer_processor.safetensors
train_config.json
```

## 3. 创建本轮独立导出目录

```bash
rm -rf ~/act_pick_blue_box_v2_83ep_export
mkdir -p ~/act_pick_blue_box_v2_83ep_export
```

## 4. 导出 20k / 25k / 30k

```bash
cp -a ~/outputs/train/act_pick_blue_box_v2_83ep/checkpoints/020000/pretrained_model   ~/act_pick_blue_box_v2_83ep_export/020000

cp -a ~/outputs/train/act_pick_blue_box_v2_83ep/checkpoints/025000/pretrained_model   ~/act_pick_blue_box_v2_83ep_export/025000

cp -a ~/outputs/train/act_pick_blue_box_v2_83ep/checkpoints/030000/pretrained_model   ~/act_pick_blue_box_v2_83ep_export/030000
```

检查：

```bash
ls -lah ~/act_pick_blue_box_v2_83ep_export
ls -lh ~/act_pick_blue_box_v2_83ep_export/*/model.safetensors
```

## 5. 保存版本信息

```bash
git -C ~/lerobot-main rev-parse HEAD   > ~/act_pick_blue_box_v2_83ep_export/lerobot_commit.txt
```

当前固定 commit：

```text
4aaff99be4a1d81568c08c8f0296b41b40c99ec4
```

环境版本：

```bash
python - <<'PY' > ~/act_pick_blue_box_v2_83ep_export/versions.txt
import sys
import torch
import torchvision
import torchcodec
import av
import importlib.metadata

print("Python:", sys.version)
print("LeRobot:", importlib.metadata.version("lerobot"))
print("PyTorch:", torch.__version__)
print("TorchVision:", torchvision.__version__)
print("CUDA runtime:", torch.version.cuda)
print("CUDA available:", torch.cuda.is_available())
print("GPU:", torch.cuda.get_device_name(0) if torch.cuda.is_available() else "NONE")
print("TorchCodec:", torchcodec.__version__)
print("PyAV:", av.__version__)
PY
```

## 6. 保存训练摘要

```bash
cat > ~/act_pick_blue_box_v2_83ep_export/training_summary.txt <<'EOF'
Experiment: ACT Pick Blue Box v2
Run name: act_pick_blue_box_v2_83ep

Dataset:
Episodes: 83
Frames: 34248
FPS: 30
Task: Pick up the blue box.
Cameras: overhead, right_wrist
State: 6D
Action: 6D

Training:
Policy: ACT
Batch size: 8
Steps: 30000
Approx epochs: 7.01
Save frequency: 5000
Chunk size: 100
n_action_steps: 100
Learning rate: 1e-5

Exported checkpoints:
020000
025000
030000
EOF
```

## 7. 打包

```bash
cd ~
tar -czf act_pick_blue_box_v2_83ep_20k_25k_30k.tar.gz   act_pick_blue_box_v2_83ep_export
```

验证：

```bash
tar -tzf ~/act_pick_blue_box_v2_83ep_20k_25k_30k.tar.gz | grep model.safetensors
```

## 8. SHA256

服务器：

```bash
sha256sum ~/act_pick_blue_box_v2_83ep_20k_25k_30k.tar.gz
```

Windows：

```powershell
Get-FileHash F:\260826_so101\models\act_v2_83ep\act_pick_blue_box_v2_83ep_20k_25k_30k.tar.gz -Algorithm SHA256
```

两边必须一致。

## 9. 下载到 Windows

```powershell
New-Item -ItemType Directory -Force F:\260826_so101\models\act_v2_83ep
$SSH_PORT = 19987
scp -P $SSH_PORT featurize@workspace.featurize.cn:/home/featurize/act_pick_blue_box_v2_83ep_20k_25k_30k.tar.gz F:\260826_so101\models\act_v2_83ep\
```

注意：

- SSH 端口每次实例可能变化，以当前 Featurize 页面为准。
- 不要写成 `-P <19987>`；尖括号只是文档占位写法。

## 10. Windows 解压

```powershell
tar -xzf F:\260826_so101\models\act_v2_83ep\act_pick_blue_box_v2_83ep_20k_25k_30k.tar.gz `
  -C F:\260826_so101\models\act_v2_83ep
```

部署时推荐整理为：

```text
F:\260826_so101\models\act_pick_blue_box_v2_83ep_export
├── 020000
├── 025000
├── 030000
├── lerobot_commit.txt
├── versions.txt
└── training_summary.txt
```

## 11. 固定原则

1. 每轮使用独立导出目录，不覆盖旧模型。
2. 至少保留 3 个后期 checkpoint 做真机比较。
3. 导出完整 `pretrained_model`，不要只复制 `model.safetensors`。
4. 保存 commit、环境版本和训练摘要。
5. 下载前后进行 SHA256 校验。
