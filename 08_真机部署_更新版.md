# 真机部署

> 更新日期：2026-08-30  
> 状态：ACT v1 已完成 Windows 本机真机 rollout 验证。  
> 本文只保留当前已验证的部署流程和故障处理。

## 1. 当前部署对象

模型目录：

```text
F:\260826_so101\models\act_pick_blue_box_export
├── 006000
├── 008000
└── 010000
```

当前重点模型：

```text
008000
010000
```

008000 已确认包含完整 LeRobot pretrained policy 文件：

```text
config.json
model.safetensors
policy_preprocessor.json
policy_preprocessor_step_3_normalizer_processor.safetensors
policy_postprocessor.json
policy_postprocessor_step_0_unnormalizer_processor.safetensors
train_config.json
```

## 2. 当前部署环境

```text
环境名       lerobot-so101-clean
环境路径     F:\miniconda3\envs\lerobot-so101-clean
Python       3.12.14
LeRobot      0.6.2
Commit       4aaff99be4a1d81568c08c8f0296b41b40c99ec4
PyTorch      2.10.0+cu126
TorchVision  0.25.0+cu126
TorchCodec   0.10.0
PyAV         15.1.0
FFmpeg       7.1.1
GPU          NVIDIA GeForce RTX 4060 Laptop GPU
CUDA         available
```

云端训练使用 cu128，本地部署使用 cu126。008000 已实际成功加载并完成真机控制，因此当前无需为部署强行升级本地 CUDA/PyTorch。

## 3. 部署前检查

```cmd
conda activate lerobot-so101-clean
cd /d F:\260826_so101
```

检查 LeRobot：

```cmd
pip show lerobot
```

检查 CUDA：

```cmd
python -c "import torch; print('Torch:',torch.__version__); print('CUDA:',torch.cuda.is_available()); print('GPU:',torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'NONE')"
```

当前已验证输出：

```text
LeRobot 0.6.2
Torch 2.10.0+cu126
CUDA True
GPU NVIDIA GeForce RTX 4060 Laptop GPU
```

## 4. 模型只读加载

配置检查：

```cmd
python -c "from lerobot.policies.act.configuration_act import ACTConfig; c=ACTConfig.from_pretrained(r'F:\260826_so101\models\act_pick_blue_box_export\008000'); print(c)"
```

完整权重加载：

```cmd
python -c "from lerobot.policies.act import ACTPolicy; p=ACTPolicy.from_pretrained(r'F:\260826_so101\models\act_pick_blue_box_export\008000'); print('MODEL LOAD OK'); print(p.config)"
```

当前已验证：

```text
MODEL LOAD OK
```

ACT 输入：

```text
observation.state                 6D
observation.images.overhead       3 x 480 x 640
observation.images.right_wrist    3 x 480 x 640
```

ACT 输出：

```text
action                            6D
```

关键配置：

```text
chunk_size = 100
n_action_steps = 100
temporal_ensemble_coeff = None
```

## 5. 确认 Follower 串口

每次部署前：

```cmd
lerobot-find-port
```

2026-08-30 当前会话确认：

```text
右 Leader   COM7
右 Follower COM3
```

实际部署：

```text
--robot.port=COM3
--robot.id=so101_right_follower
```

> COM 号可能因 USB 重插变化；`robot.id` 不要随意修改，否则 calibration 身份会变化。

## 6. 确认摄像头

```cmd
lerobot-find-cameras
```

2026-08-30 当前会话发现 OpenCV Camera 0/1/2，其中实际部署使用：

```text
overhead     = camera 1
right_wrist  = camera 2
```

部署必须保持模型训练 key：

```text
overhead
right_wrist
```

不要改成历史测试 key：

```text
top
wrist
```

分辨率和 FPS：

```text
640 x 480
30 FPS
```

## 7. 首次 5 秒安全验证

建议第一次连接新 checkpoint 时先短时运行：

```cmd
lerobot-rollout --strategy.type=base --policy.path="F:\260826_so101\models\act_pick_blue_box_export\008000" --robot.type=so101_follower --robot.port=COM3 --robot.id=so101_right_follower --robot.cameras="{overhead: {type: opencv, index_or_path: 1, width: 640, height: 480, fps: 30}, right_wrist: {type: opencv, index_or_path: 2, width: 640, height: 480, fps: 30}}" --task="Pick up the blue box." --fps=30 --duration=5 --device=cuda --return_to_initial_position=false
```

当前已验证：

- policy 正常加载；
- Camera 1/2 正常连接；
- SO-101 右 Follower 正常连接；
- sync inference 正常启动；
- 真机实际产生动作；
- 5 秒后正常退出。

## 8. 正式 25 秒 rollout

008000：

```cmd
lerobot-rollout --strategy.type=base --policy.path="F:\260826_so101\models\act_pick_blue_box_export\008000" --robot.type=so101_follower --robot.port=COM3 --robot.id=so101_right_follower --robot.cameras="{overhead: {type: opencv, index_or_path: 1, width: 640, height: 480, fps: 30}, right_wrist: {type: opencv, index_or_path: 2, width: 640, height: 480, fps: 30}}" --task="Pick up the blue box." --fps=30 --duration=25 --device=cuda --return_to_initial_position=True
```

010000：

```cmd
lerobot-rollout --strategy.type=base --policy.path="F:\260826_so101\models\act_pick_blue_box_export\010000" --robot.type=so101_follower --robot.port=COM3 --robot.id=so101_right_follower --robot.cameras="{overhead: {type: opencv, index_or_path: 1, width: 640, height: 480, fps: 30}, right_wrist: {type: opencv, index_or_path: 2, width: 640, height: 480, fps: 30}}" --task="Pick up the blue box." --fps=30 --duration=25 --device=cuda --return_to_initial_position=True
```

### 关于 `--task`

ACT v1 的 input features 不包含语言，因此 task 字符串不会像 VLA 那样直接驱动策略。为了项目元数据统一，后续仍统一使用训练数据的：

```text
Pick up the blue box.
```

## 9. `return_to_initial_position`

```text
--return_to_initial_position=True
```

rollout 结束后平滑返回启动时捕获的关节位置，适合重复固定条件实验。

首次安全 smoke 或相机故障排查时可以：

```text
--return_to_initial_position=false
```

避免异常退出后继续产生自动回位动作。

## 10. 不再使用 `n_action_steps=25`

对 008000 做过：

```text
--policy.n_action_steps=25
```

真机表现变差：原本默认 100 时能够稳定向物块靠近；改为 25 后未形成有效靠近轨迹，并持续出现约 12~14 Hz 的慢循环警告。

当前结论：

```text
保持模型训练配置：n_action_steps=100
```

除非重新训练或有明确实验目的，不再在部署时覆盖为 25。

## 11. Camera 2 stale frame 故障

曾出现：

```text
TimeoutError: OpenCVCamera(2) latest frame is too old: 523.8 ms (max allowed: 500 ms)
```

含义：

```text
right_wrist Camera 2
→ 超过 500 ms 没有更新新帧
→ LeRobot read_latest() 主动报 TimeoutError
→ rollout 提前停止
```

这不是 ACT 权重错误。

排查顺序：

1. 关闭 OBS、Windows Camera、浏览器等摄像头占用程序。
2. 检查腕部 USB 线是否在机械臂运动时被拉扯。
3. 尽量避免两路摄像头通过不稳定 Hub 共用同一连接。
4. 单独测试 Camera 2 长时间取帧。
5. 若重复发生，可尝试 OpenCV `fourcc: MJPG` 降低 USB 带宽压力。

之后连续多次 25 秒 rollout 均完整结束，所以当前记录为偶发相机故障，继续监控。

## 12. Windows CMD 多行命令注意

CMD 中：

```text
More?
```

是续行提示，不是命令内容。

曾因把终端输出中的 `More?` 一并复制回命令导致：

```text
lerobot-rollout: error: unrecognized arguments: More? More? ...
```

为了避免该问题，当前优先使用本文给出的一整行命令。

## 13. 当前模型行为结论

008000：

```text
会向物块靠近
→ 进入物块附近
→ 夹爪未稳定处于物块两侧
→ 在附近徘徊
→ 未完成抓取
```

010000：

```text
同样会靠近
→ 仍未完成正确夹取
```

因此当前部署本身已验证成功，下一阶段不再把重点放在 rollout 命令，而是转向数据质量和 ACT v2 训练。

## 14. 安全原则

- 首次使用新 checkpoint 先短时运行。
- 清空机械臂周围障碍物。
- 保持训练时相机安装位置和画面方向。
- 人保持在电源/急停附近。
- 出现异常运动立即停止。
- 不要在真机 autonomous rollout 中随意修改 calibration、舵机 ID 或控制底层配置。
