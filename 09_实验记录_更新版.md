# 实验记录

> 更新日期：2026-08-30

# ACT v1：右臂抓取蓝色方块

## 1. 实验基准

| 项目 | 记录 |
|---|---|
| 实验名称 | ACT Pick Blue Box v1 |
| 数据集 repo_id | `meyumei/so101-right-pick-blue-box-v1-70ep` |
| 实际 Episodes | 30 |
| Frames | 12,660 |
| FPS | 30 |
| Cameras | `overhead`, `right_wrist` |
| State | 6D |
| Action | 6D |
| Policy | ACT |
| Chunk size | 100 |
| `n_action_steps` | 100 |
| Batch Size | 8 |
| Steps | 10,000 |
| Steps / Epoch | ≈1,583 |
| 等效 Epoch | ≈6.32 |
| Checkpoints | 2k / 4k / 6k / 8k / 10k |
| LeRobot | 0.6.2 |
| LeRobot Commit | `4aaff99be4a1d81568c08c8f0296b41b40c99ec4` |
| 训练 PyTorch | 2.10.0+cu128 |
| 本地部署 PyTorch | 2.10.0+cu126 |
| 训练 GPU | RTX 4090 |
| 部署 GPU | RTX 4060 Laptop GPU |
| 训练时间 | ≈13m15s |
| Smoke Test | 100 steps，成功 |
| 正式训练 | 10,000 steps，成功 |

## 2. Loss 观察

后期约 8k~10k：

```text
loss 大致进入 0.19~0.23 附近小幅波动
```

训练过程正常，但 training loss 不能代表真机抓取成功率。

## 3. 部署环境验证

2026-08-30 本地部署确认：

```text
LeRobot      0.6.2
Commit       4aaff99be4a1d81568c08c8f0296b41b40c99ec4
Torch        2.10.0+cu126
CUDA         True
GPU          NVIDIA GeForce RTX 4060 Laptop GPU
Follower     COM3 / so101_right_follower
overhead     camera 1
right_wrist  camera 2
```

008000 完整权重加载：

```text
MODEL LOAD OK
```

模型 feature：

```text
observation.state                 (6,)
observation.images.overhead       (3, 480, 640)
observation.images.right_wrist    (3, 480, 640)
action                            (6,)
```

## 4. 真机实验记录

### E01：008000 / 5 秒 deployment smoke

- Checkpoint：`008000`
- `n_action_steps`：100（默认）
- Duration：5 s
- 结果：真机成功产生自主动作。
- 结论：模型加载、两路相机、Follower、CUDA、sync inference、action 下发链路全部打通。
- 该实验只验证部署，不评价抓取成功率。

### E02：008000 / 15 秒探索测试

- Checkpoint：`008000`
- Duration：15 s
- 现象：机械臂能够向蓝色方块靠近，但未成功抓取。
- 初步判断：策略已经学到大范围 reaching，但抓取精度不足。

### E03：008000 / Camera 2 stale frame 中断

- 计划 Duration：25 s
- 实际运行：约 3.8 s 后中断。
- 报错：

```text
TimeoutError: OpenCVCamera(2) latest frame is too old: 523.8 ms (max allowed: 500 ms)
```

- 原因：`right_wrist` Camera 2 超过 500 ms 没有更新新帧。
- 分类：部署/相机故障，不计入策略抓取失败统计。
- 后续：之后连续多次 25 秒运行完整结束，因此当前记录为偶发故障。

### E04~E07：008000 / 25 秒 / 多个采集位置系统测试

用户将蓝色物块放回采集时的多个典型位置，共进行 4 次重点评估。

共同现象：

```text
机械臂会主动向物块靠近
→ 到达物块附近
→ 末端/夹爪在物块周围调整或徘徊
→ 夹爪没有一次稳定位于物块两侧
→ 未完成夹取
```

统计：

| 指标 | 结果 |
|---|---:|
| 测试次数 | 4 |
| 会向物块靠近 | 4 / 4 |
| 夹爪正确进入物块两侧 | 0 / 4 |
| 完整夹取成功 | 0 / 4 |

四次 25 秒日志均完整运行到 Duration limit，整体 effective cadence 约：

```text
28.34 Hz
28.34 Hz
28.43 Hz
28.58 Hz
```

结论：硬件/rollout 链路稳定；失败模式高度一致，不像随机部署错误。

### E08：008000 / `n_action_steps=25`

测试覆盖参数：

```text
--policy.n_action_steps=25
```

行为：

```text
相比默认 100 明显变差
甚至没有形成原本稳定的靠近物体轨迹
```

运行特征：

- 持续出现约 12~14 Hz 的慢循环 warning；
- 更频繁触发完整 ACT forward；
- 真机行为没有改善精细抓取，反而破坏原有 reaching。

结论：

```text
本项目 ACT v1 部署保持 n_action_steps=100
不再使用 25 作为默认 rollout 参数
```

### E09：010000 / 默认 `n_action_steps=100`

- Checkpoint：`010000`
- 现象：同样能够向物块靠近，但仍没有形成正确夹取。
- 夹爪没有解决“稳定进入物块两侧”的核心问题。
- 相比 008000 未观察到足以改变阶段结论的改善。

> 当前没有把 010000 写成精确成功率，因为本轮记录中没有完整的固定次数统计；已确认至少一次代表性真机验证表现与 008000 同类。

### 006000

截至本版报告生成时：尚未形成完整系统评估记录。

当前已经由 008000 和 010000 的一致失败模式决定优先转向数据改进，因此 006000 不再是当前最高优先级。

## 5. Checkpoint 阶段结论

| Checkpoint / 设置 | Reaching | 精细对准 | 完整抓取 | 当前判断 |
|---|---|---|---|---|
| 008000 / 100 | 稳定出现 | 不稳定 | 0/4 系统测试 | 学到接近，未学稳抓取 |
| 008000 / 25 | 明显变差 | 无改善 | 失败 | 不采用 |
| 010000 / 100 | 会靠近 | 仍不稳定 | 未成功 | 未解决核心问题 |
| 006000 | 未系统评估 | - | - | 暂缓 |

## 6. 当前失败模式

当前最稳定的失败模式不是“看不到物块”，而是：

```text
看到相关区域
→ 能产生向物块靠近的动作
→ 到达近距离后无法形成稳定抓取几何关系
→ gripper 中心/方向没有对准物块
→ 夹爪没有稳定处于物块两侧
→ 在附近徘徊或错过抓取
```

因此当前问题应描述为：

```text
reaching learned
fine-grained grasp alignment insufficient
```

不应简单写成“目标识别失败”。

## 7. 当前项目结论

综合：

- 30 episodes；
- 12,660 frames；
- 10k steps；
- 008000 多位置 0/4 抓取；
- 010000 仍为相同失败模式；
- 调小 `n_action_steps` 没有改善。

本项目当前决定：

```text
ACT v1 数据不足以稳定学习精细夹取
```

这里的“数据不足”不仅指 episode 数量，还包括最后抓取阶段动作的一致性、精度和覆盖。

下一步不优先继续把同一批数据训练到更高 steps，而是建立 ACT v2 数据集。

## 8. ACT v2 数据目标

保持硬件与 feature 不变：

```text
SO-101 right follower
overhead + right_wrist
640 x 480 @ 30 FPS
6D state
6D action
Pick up the blue box.
```

重点加强：

```text
最后 5~10 cm 接近
→ wrist / gripper 精确对准
→ 夹爪两侧包住物块
→ 闭合
→ 短暂稳定
→ 抬升
```

采集要求：

1. 抓取方向尽量一致。
2. wrist_roll 不要在同一场景出现大量完全不同策略。
3. 闭合时机明确。
4. 夹偏、撞击、失败、掉帧 episode 重录。
5. 物块位置可以变化，但每个位置尽量使用一致的成功策略。
6. 正式批次前逐条检查最后抓取阶段，而不是只检查视频文件能否播放。

## 9. 下一实验模板

```markdown
### Experiment ID

- 日期：
- 数据集：
- Episodes：
- Frames：
- LeRobot：
- Commit：
- Policy：
- Batch size：
- Steps：
- Save freq：
- 训练时间：
- 关键 loss：
- 使用 checkpoint：
- n_action_steps：
- 真机测试次数：
- 成功次数：
- 成功率：
- reaching 成功次数：
- gripper alignment 成功次数：
- 失败类型：
- 相机故障：
- 备注：
```
