# 遥操作与摄像头

## 1. 安全顺序



发现

lerobot-find-cameras

推荐顺序：

```text
识别端口
→ 确认旧配置 / 校准
→ 空载遥操作
→ 接入相机
→ 本地小样本录制
→ 检查数据
→ 正式采集
```

不要在未确认硬件状态时直接开始正式采集。

---

## 2. 右臂遥操作历史命令

```cmd
lerobot-teleoperate --fps=30 --teleop.type=so101_leader --teleop.port=COM7 --teleop.id=so101_right_leader --robot.type=so101_follower --robot.port=COM3 --robot.id=so101_right_follower
```

---

## 3. 左臂首次安全测试原则

首次测试左侧机械臂时可只连接：

```text
左 Leader   so101_left_leader
左 Follower so101_left_follower
```

先不要连接右臂，也先不要接摄像头。

---

## 4. 摄像头

连接摄像头后，可先使用 OBS 调整：

- 角度
- 曝光
- 画面构图
- 遮挡
- 机械臂是否完整出现在视野内

但在 LeRobot 中还需要确认 OpenCV 相机编号。

建议记录：

```text
overhead     = camera ?
right_wrist  = camera ?

width  = 640
height = 480
fps    = 30
```

---

## 5. 相机 key 一致性

历史测试命令曾使用：

```text
top
wrist
```

但当前 ACT 训练数据实际使用：

```text
observation.images.overhead
observation.images.right_wrist
```

因此后续正式采集与真机部署建议统一使用：

```text
overhead
right_wrist
```

避免再进行中间重命名。

---

## 6. 注意

相机编号可能因 USB 插拔顺序变化。

因此每次采集或部署前，都应重新确认：

```text
overhead 对应哪个 index
right_wrist 对应哪个 index
```
