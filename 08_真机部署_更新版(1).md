# 真机部署

> 当前基准：ACT v2，83 episodes，20k / 25k / 30k checkpoint。  
> 当前部署环境为 Windows `lerobot-so101-clean`。

## 1. 当前部署环境

```text
环境名       lerobot-so101-clean
环境路径     F:\miniconda3\envs\lerobot-so101-clean
Python       3.12.14
LeRobot      0.6.2
Commit       4aaff99be4a1d81568c08c8f0296b41b40c99ec4
PyTorch      2.10.0+cu126
TorchVision  0.25.0+cu126
TorchCodec   0.10.0
PyAV         15.1.0
FFmpeg       7.1.1
GPU          NVIDIA GeForce RTX 4060 Laptop GPU
```

已验证：

```text
CUDA available = True
```

## 2. 当前 v2 模型目录

推荐：

```text
F:\260826_so101\models\act_pick_blue_box_v2_83ep_export
├── 020000
├── 025000
└── 030000
```

每个 checkpoint 目录应至少包含：

```text
config.json
model.safetensors
policy_preprocessor.json
policy_postprocessor.json
train_config.json
```

## 3. 先检查模型能否本地加载

以 25k 为例：

```cmd
python -c "from lerobot.policies.act import ACTPolicy; p=ACTPolicy.from_pretrained(r'F:\260826_so101\models\act_pick_blue_box_v2_83ep_export\025000'); print('MODEL LOAD OK'); print(p.config)"
```

看到：

```text
MODEL LOAD OK
```

再进入真机 rollout。

## 4. 当前机器人与相机配置

最近已验证：

```text
右 Follower COM    = COM3
robot.id            = so101_right_follower
overhead             = camera 1
right_wrist          = camera 2
```

每次 USB 重插后仍建议检查：

```cmd
lerobot-find-port
lerobot-find-cameras
```

相机 key 必须保持训练时：

```text
overhead
right_wrist
```

分辨率与 FPS：

```text
640 x 480
30 FPS
```

## 5. Windows 路径格式必须正确

本地模型路径必须是标准 Windows 绝对路径：

```text
F:\260826_so101\models\...
```

错误示例：

```text
F\260826_so101\models\...
```

这里少了盘符后的冒号 `:`。

如果少了 `:`，LeRobot 可能不会把它识别为本地路径，而会把字符串交给 Hugging Face Hub 解析，随后出现：

```text
HFValidationError: Repo id must use ...
```

所以 `--policy.path` 必须确认包含：

```text
F:\
```

而不是：

```text
F\
```

## 6. 25k 首次 5 秒安全测试

```cmd
lerobot-rollout --strategy.type=base --policy.path="F:\260826_so101\models\act_pick_blue_box_v2_83ep_export\025000" --robot.type=so101_follower --robot.port=COM3 --robot.id=so101_right_follower --robot.cameras="{overhead: {type: opencv, index_or_path: 1, width: 640, height: 480, fps: 30}, right_wrist: {type: opencv, index_or_path: 2, width: 640, height: 480, fps: 30}}" --task="Pick up the blue box." --fps=30 --duration=5 --device=cuda --return_to_initial_position=True
```

首次新 checkpoint 测试建议先保持：

```text
duration = 5
```

确认：

- 模型加载成功；
- 两路相机连接；
- Follower 连接；
- 机械臂运动方向正常；
- 没有异常动作。

## 7. 正式 25 秒 rollout

25k：

```cmd
lerobot-rollout --strategy.type=base --policy.path="F:\260826_so101\models\act_pick_blue_box_v2_83ep_export\025000" --robot.type=so101_follower --robot.port=COM3 --robot.id=so101_right_follower --robot.cameras="{overhead: {type: opencv, index_or_path: 1, width: 640, height: 480, fps: 30}, right_wrist: {type: opencv, index_or_path: 2, width: 640, height: 480, fps: 30}}" --task="Pick up the blue box." --fps=30 --duration=25 --device=cuda --return_to_initial_position=True
```

20k 只改：

```text
025000 → 020000
```

30k 只改：

```text
025000 → 030000
```

## 8. 不覆盖 `n_action_steps`

本轮 v2 训练使用：

```text
chunk_size = 100
n_action_steps = 100
```

部署时保持 checkpoint 自身配置，不额外添加：

```text
--policy.n_action_steps=25
```

因为上一轮真机对照实验表明，覆盖成 25 会明显破坏原本的 reaching 行为。

## 9. `--task`

当前 ACT 的输入 feature 不包含语言，因此 `--task` 不像 VLA 那样直接驱动动作。

为了和数据集元数据一致，统一使用：

```text
Pick up the blue box.
```

## 10. 评估顺序

建议固定同一组物块位置和机械臂初始姿态，分别测试：

```text
020000
025000
030000
```

记录：

```text
是否靠近
是否正确对准
夹爪是否位于物块两侧
是否闭合
是否抓住
是否抬起
```

最终 checkpoint 以真机结果为准，不只看训练 loss。

## 11. 常见部署错误

### 11.1 Hugging Face `HFValidationError`

优先检查：

```text
--policy.path
```

是否为正确的 Windows 本地路径，并确认有：

```text
F:\
```

### 11.2 Camera stale frame

如果出现：

```text
OpenCVCamera(2) latest frame is too old
```

优先检查：

- 腕部相机 USB 线；
- 摄像头是否被其他软件占用；
- USB Hub / 接口稳定性；
- camera index 是否变化。

### 11.3 CMD `More?`

`More?` 是 Windows CMD 多行续行提示，不是命令内容。

不要把：

```text
More?
```

复制回命令。

当前优先使用单行 `lerobot-rollout` 命令。

## 12. 安全原则

- 新 checkpoint 先做 5 秒测试。
- 蓝色方块先放在训练数据覆盖的常见位置。
- 保持采集时相机安装位置和方向。
- 清空机械臂周围障碍物。
- 人保持在电源 / 急停附近。
- 出现异常动作立即停止。
