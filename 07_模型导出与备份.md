# 模型导出与备份

## 1. 查看 checkpoint

```bash
conda activate lerobot-so101-train
cd ~/lerobot-so101

ls -lah outputs/train/act_pick_blue_box_v1/checkpoints
```

当前：

```text
002000
004000
006000
008000
010000
last -> 010000
```

---

## 2. 创建导出目录

```bash
rm -rf ~/act_pick_blue_box_export
mkdir -p ~/act_pick_blue_box_export
```

---

## 3. 导出 6k / 8k / 10k

```bash
cp -a   outputs/train/act_pick_blue_box_v1/checkpoints/006000/pretrained_model   ~/act_pick_blue_box_export/006000

cp -a   outputs/train/act_pick_blue_box_v1/checkpoints/008000/pretrained_model   ~/act_pick_blue_box_export/008000

cp -a   outputs/train/act_pick_blue_box_v1/checkpoints/010000/pretrained_model   ~/act_pick_blue_box_export/010000
```

每个 `pretrained_model` 约 198 MB。

---

## 4. 保存 LeRobot commit

```bash
git -C ~/lerobot-main rev-parse HEAD   > ~/act_pick_blue_box_export/lerobot_commit.txt
```

当前：

```text
4aaff99be4a1d81568c08c8f0296b41b40c99ec4
```

---

## 5. 保存环境版本

```bash
python - <<'PY' > ~/act_pick_blue_box_export/versions.txt
import sys
import torch
import torchvision
import torchcodec
import av
import importlib.metadata

print("Python:", sys.version)
print("LeRobot:", importlib.metadata.version("lerobot"))
print("PyTorch:", torch.__version__)
print("torchvision:", torchvision.__version__)
print("CUDA runtime:", torch.version.cuda)
print("TorchCodec:", torchcodec.__version__)
print("PyAV:", av.__version__)
PY
```

当前记录：

```text
Python       3.12.14
LeRobot      0.6.2
PyTorch      2.10.0+cu128
torchvision  0.25.0+cu128
CUDA runtime 12.8
TorchCodec   0.10.0
PyAV         15.1.0
```

---

## 6. 打包

```bash
cd ~
tar -czf act_pick_blue_box_v1_6k_8k_10k.tar.gz act_pick_blue_box_export
```

验证：

```bash
tar -tzf ~/act_pick_blue_box_v1_6k_8k_10k.tar.gz | head -30
```

---

## 7. 下载到 Windows

在 Windows PowerShell：

```powershell
scp -P <当前SSH端口> `
  featurize@workspace.featurize.cn:/home/featurize/act_pick_blue_box_v1_6k_8k_10k.tar.gz `
  F:\260826_so101\models\
```

> SSH 端口必须使用当前实例实际端口，不要永久写死。

---

## 8. 解压

```powershell
tar -xzf F:\260826_so101\models\act_pick_blue_box_v1_6k_8k_10k.tar.gz `
  -C F:\260826_so101\models
```

当前本地目录：

```text
F:\260826_so101\models\act_pick_blue_box_export
├── 006000
├── 008000
├── 010000
├── lerobot_commit.txt
└── versions.txt
```

---

## 9. 建议增加 SHA256 校验

服务器：

```bash
sha256sum ~/act_pick_blue_box_v1_6k_8k_10k.tar.gz
```

Windows：

```powershell
Get-FileHash F:\260826_so101\models\act_pick_blue_box_v1_6k_8k_10k.tar.gz -Algorithm SHA256
```

两边应完全一致。
