# 硬件与环境

## 1. Windows 本机环境检查

在 PowerShell 中可检查：

```powershell
Get-CimInstance Win32_OperatingSystem
Get-CimInstance Win32_Processor
Get-CimInstance Win32_ComputerSystem
nvidia-smi
python --version
git --version
conda --version
ffmpeg --version
```

---

## 2. 回退备份环境（历史采集实际使用，当前不默认使用）

这一节记录升级前的 LeRobot 0.6.1 环境。对应的回退环境现保存在：

```text
F:\miniconda3\envs\lerobot-so101-clean-backup
```

它用于追溯旧采集流程或在明确需要时回退，不是当前新采集和真机部署的默认环境。

创建环境：

```cmd
conda create -n lerobot-so101 python=3.12 pip -y
conda activate lerobot-so101
```

安装 GPU 版 PyTorch：

```cmd
pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu126
```

检查：

```cmd
pip show torch torchvision
```

安装 FFmpeg：

```cmd
conda install -y -c conda-forge ffmpeg
ffmpeg --version
```

安装 LeRobot：

```cmd
pip install "lerobot[core_scripts,feetech]==0.6.1"
```

### 历史采集/备份环境确认

```text
Python       3.12.14
LeRobot      0.6.1
PyTorch      2.10.0+cu126
TorchVision  0.25.0+cu126
TorchCodec   0.10.0
FFmpeg       8.1.2
Feetech SDK  1.0.0
PySerial     3.5
```

默认 LeRobot 数据缓存：

```text
F:\260826_so101\huggingface\lerobot
```

默认校准文件目录：

```text
F:\260826_so101\huggingface\lerobot\calibration
```

---

## 3. 当前 Windows 本地工作环境

当前新采集、模型加载和真机部署默认使用：

```text
环境名       lerobot-so101-clean
环境路径     F:\miniconda3\envs\lerobot-so101-clean
Python       3.12.14
LeRobot      0.6.2
Commit       4aaff99be4a1d81568c08c8f0296b41b40c99ec4
PyTorch      2.10.0+cu126
TorchVision  0.25.0+cu126
TorchCodec   0.10.0
PyAV         15.1.0
FFmpeg       7.1.1
```

详细基准和升级边界见 [00_项目总览.md](./00_项目总览.md)。不要把本节当前环境与上一节 0.6.1 备份环境混用。

---

## 4. 训练服务器环境（ACT v1）

创建隔离环境：

```bash
conda create -n lerobot-so101-train python=3.12 pip -y
conda activate lerobot-so101-train
```

安装 PyTorch：

```bash
pip install   torch==2.10.0   torchvision==0.25.0   --index-url https://download.pytorch.org/whl/cu128
```

安装 FFmpeg：

```bash
conda install -y ffmpeg=7.1.1 -c conda-forge
```

安装 LeRobot 源码：

```bash
cd ~
git clone https://github.com/huggingface/lerobot.git lerobot-main
cd ~/lerobot-main
git checkout 4aaff99be4a1d81568c08c8f0296b41b40c99ec4
pip install -e ".[training]"
```

固定 TorchCodec：

```bash
pip install --force-reinstall torchcodec==0.10.0
```

检查版本：

```bash
python - <<'PY'
import torch
import torchvision
import torchcodec
import av
import importlib.metadata

print("LeRobot:", importlib.metadata.version("lerobot"))
print("PyTorch:", torch.__version__)
print("torchvision:", torchvision.__version__)
print("CUDA runtime:", torch.version.cuda)
print("TorchCodec:", torchcodec.__version__)
print("PyAV:", av.__version__)
print("CUDA available:", torch.cuda.is_available())

if torch.cuda.is_available():
    print("GPU:", torch.cuda.get_device_name(0))
PY
```

---

## 5. 概念说明

### port

`port` 是操作系统识别出的串口设备，例如：

```text
COM3
COM7
```

它不是“某个固定物理 USB 孔”。

### robot.id / teleop.id

它们是整台机器人或遥操作设备的标识，例如：

```text
so101_right_follower
so101_right_leader
```

### 舵机 ID

SO101 的 6 个舵机一般在总线上使用 1～6 的编号。

当前软件关节顺序：

```text
1 shoulder_pan
2 shoulder_lift
3 elbow_flex
4 wrist_flex
5 wrist_roll
6 gripper
```
