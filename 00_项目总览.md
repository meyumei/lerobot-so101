# SO-101 ACT 项目总览与环境基准

> 基准日期：2026-08-30  
> 项目根目录：`F:\260826_so101`  
> 本文件由原 `00_项目总览.md` 与 `00环境版本.md` 整理合并，是项目状态与本地环境的唯一总入口。  
> 其他文档若出现旧版本、旧路径或待确认参数，以本文件中标注为“当前已验证”的内容为准。

## 1. 项目目标

使用 Windows 本机连接 SO-101 Leader/Follower 和两路摄像头，完成右侧单臂视觉抓取的完整模仿学习流程：

```text
机械臂与相机准备
→ Leader 遥操作 Follower
→ 采集同步示范数据
→ 检查和整理数据
→ 云端训练 ACT
→ 导出 checkpoint
→ Windows 本机加载模型
→ 真机部署与成功率评估
```

当前任务：

```text
右侧 SO-101 Follower 根据顶视和腕部相机画面抓取蓝色盒子。
任务文本：Pick up the blue box.
```

当前不是双臂同步训练任务。现阶段使用右侧一套 Leader/Follower 完成单臂采集、训练和部署；左侧机械臂保留供后续实验使用。

## 2. 文档导航

| 文档 | 作用 |
|---|---|
| [00_项目总览.md](./00_项目总览.md) | 当前项目事实、环境基准、进度和统一约束 |
| [01_硬件与环境.md](./01_硬件与环境.md) | 硬件概念、备份环境和云端环境记录 |
| [02_机械臂配置与校准.md](./02_机械臂配置与校准.md) | COM、设备 ID、舵机和校准说明 |
| [03_遥操作与摄像头.md](./03_遥操作与摄像头.md) | 遥操作流程与相机配置 |
| [04_数据采集.md](./04_数据采集.md) | Smoke test 和正式采集说明 |
| [05_数据检查与整理.md](./05_数据检查与整理.md) | 数据结构、视频和加载检查 |
| [06_ACT训练.md](./06_ACT训练.md) | 云端 ACT 训练命令与结果 |
| [07_模型导出与备份.md](./07_模型导出与备份.md) | Checkpoint 导出、传输和校验 |
| [08_真机部署.md](./08_真机部署.md) | 真机部署准备和安全要求 |
| [09_实验记录.md](./09_实验记录.md) | ACT v1 结果及后续实验记录模板 |

### 文档时效说明

- `01_硬件与环境.md` 中的 LeRobot 0.6.1 / FFmpeg 8.1.2 是历史采集和回退备份环境，不是当前本地工作环境。
- 当前本地工作环境是 LeRobot 0.6.2、固定 commit `4aaff99...`、FFmpeg 7.1.1。
- `04_数据采集.md` 中使用 `top`、`wrist` 的命令是历史测试命令；后续正式采集使用 `overhead`、`right_wrist`。
- 云端训练使用 cu128，本地因驱动条件暂时使用 cu126；两者是不同机器的环境记录，不能混写。

## 3. 当前进度与暂停点

已完成：

- Windows 本地基础环境搭建和升级；
- 四台机械臂历史端口与固定 ID 记录；
- 右侧 Leader/Follower 校准文件确认；
- 右臂遥操作和两路相机验证；
- 右臂蓝盒抓取数据采集与数据检查；
- 云端 100-step smoke test；
- 云端 ACT 10,000-step 正式训练；
- 6k、8k、10k checkpoint 导出并下载到 F 盘；
- 本地 LeRobot 升级到训练模型对应 commit；
- 本地 FFmpeg 对齐到 7.1.1。

当前暂停点：

```text
本地环境升级完成
→ 尚未加载 ACT 模型
→ 尚未连接 Follower 和相机进行部署验证
→ 尚未进行 6k / 8k / 10k 真机比较
```

下一阶段先进行只读模型加载与输入配置核对，然后再进入短时真机 rollout。

## 4. 三套环境的身份与用途

| 环境 | 身份 | 用途 | 是否当前使用 |
|---|---|---|---|
| `lerobot-so101-clean` | 当前 Windows 本地工作环境 | 后续采集、模型加载和真机部署 | 是 |
| `lerobot-so101-clean-backup` | 升级前回退备份环境 | 必要时复现历史 0.6.1 采集环境 | 否 |
| 云端 `lerobot-so101-train` | ACT v1 训练环境 | 云 GPU 训练与 checkpoint 生成 | 云端使用 |

不要在新采集或部署时误激活 backup 环境。当前本地环境和备份环境均保存在 F 盘。

## 5. Windows 本机硬件基准（当前已验证）

| 项目 | 实际值 |
|---|---|
| 操作系统 | Windows 11 家庭版 中文版 |
| Windows 版本 | 10.0.26200 |
| 架构 | 64-bit |
| 计算机 | ASUS TUF Gaming A15 FA507NV_FA507NV |
| CPU | AMD Ryzen 7 7735H with Radeon Graphics |
| CPU 核心/线程 | 8 / 16 |
| 内存 | 约 15.25 GiB |
| GPU | NVIDIA GeForce RTX 4060 Laptop GPU |
| GPU 显存 | 8,188 MiB |
| NVIDIA 驱动 | 555.97 |
| `nvidia-smi` CUDA 上限报告 | 12.5 |

```text
torch.cuda.is_available() = True
GPU = NVIDIA GeForce RTX 4060 Laptop GPU
```

## 6. 当前 Windows 工作环境（0.6.2）

### 6.1 Conda 与路径

| 项目 | 当前已验证值 |
|---|---|
| Conda | 26.5.3 |
| Conda 根目录 | `F:\miniconda3` |
| 当前环境 | `lerobot-so101-clean` |
| 当前环境路径 | `F:\miniconda3\envs\lerobot-so101-clean` |
| Python | 3.12.14 |
| Python 程序 | `F:\miniconda3\envs\lerobot-so101-clean\python.exe` |

环境变量：

```text
HF_LEROBOT_HOME=F:\260826_so101\huggingface\lerobot
HF_LEROBOT_CALIBRATION=F:\260826_so101\huggingface\lerobot\calibration
```

### 6.2 LeRobot 源码

| 项目 | 当前已验证值 |
|---|---|
| LeRobot | 0.6.2 |
| 源码目录 | `F:\260826_so101\lerobot-0.6.2` |
| Git 状态 | detached HEAD |
| Git commit | `4aaff99be4a1d81568c08c8f0296b41b40c99ec4` |
| import 路径 | `F:\260826_so101\lerobot-0.6.2\src\lerobot\__init__.py` |
| 安装方式 | editable install |
| `pip check` | `No broken requirements found` |

这是 editable install：直接修改源码目录会立即影响采集和部署。未经记录，不要切换分支、更新 commit 或改动源码。

### 6.3 组件版本

| 组件 | 当前本地值 | 云端训练值 | 状态 |
|---|---:|---:|---|
| Python | 3.12.14 | 3.12.14 | 一致 |
| LeRobot | 0.6.2 | 0.6.2 | 一致 |
| LeRobot commit | `4aaff99...` | `4aaff99...` | 一致 |
| PyTorch | 2.10.0+cu126 | 2.10.0+cu128 | CUDA 构建不同 |
| TorchVision | 0.25.0+cu126 | 0.25.0+cu128 | CUDA 构建不同 |
| CUDA runtime | 12.6 | 12.8 | 暂未完全对齐 |
| NVIDIA 驱动 | 555.97 / CUDA 12.5 | 需支持 CUDA 12.8 | 当前不足 |
| TorchCodec | 0.10.0 | 0.10.0 | 一致 |
| PyAV | 15.1.0 | 15.1.0 | 一致 |
| FFmpeg | 7.1.1 | 7.1.1 | 一致 |
| Feetech Servo SDK | 1.0.0 | 不适用 | 已安装 |
| PySerial | 3.5 | 不适用 | 已安装 |
| NumPy | 2.2.6 | 未指定 | 已安装 |
| OpenCV | opencv-python-headless 4.13.0.92 | 采集依赖 | 已安装 |
| Datasets | 4.8.5 | 数据集依赖 | 已安装 |
| Hugging Face Hub | 1.28.0 | Hub 依赖 | 已安装 |

本地 cu126 当前 CUDA 可用。采集不要求本地 CUDA 构建与云端完全一致；首次模型加载和 rollout 需要单独验证兼容性。

在升级 NVIDIA 驱动前，不强行把本地 PyTorch 替换为 cu128。若以后需要完全对齐，顺序为：

```text
升级 NVIDIA 驱动并重启
→ 验证 nvidia-smi
→ 安装 torch / torchvision cu128
→ 验证 torch.cuda.is_available()
→ 验证模型加载
```

## 7. 回退备份环境（0.6.1）

| 项目 | 备份值 |
|---|---|
| 环境名 | `lerobot-so101-clean-backup` |
| 环境路径 | `F:\miniconda3\envs\lerobot-so101-clean-backup` |
| Python | 3.12.14 |
| LeRobot | 0.6.1 |
| PyTorch | 2.10.0+cu126 |
| TorchVision | 0.25.0+cu126 |
| TorchCodec | 0.10.0 |
| 历史 FFmpeg | 8.1.2 |
| Feetech SDK / PySerial | 1.0.0 / 3.5 |

该环境用于回退和追溯旧采集流程，不是当前默认环境。除非新环境出现明确兼容问题并经过确认，否则不应切回它继续正式采集。

## 8. 机械臂、端口与校准

| 设备 | 固定 ID | 历史 COM |
|---|---|---|
| 左 Leader | `so101_left_leader` | COM13 |
| 左 Follower | `so101_left_follower` | COM4 |
| 右 Leader | `so101_right_leader` | COM7 |
| 右 Follower | `so101_right_follower` | COM3 |

COM 口可能在重新插拔后变化；固定 ID 才是校准与设备身份标识。每次连接前重新确认端口。

当前关节顺序：

```text
1 shoulder_pan
2 shoulder_lift
3 elbow_flex
4 wrist_flex
5 wrist_roll
6 gripper
```

校准根目录：

```text
F:\260826_so101\huggingface\lerobot\calibration
```

当前文档明确确认的右侧校准文件：

```text
F:\260826_so101\huggingface\lerobot\calibration\teleoperators\so_leader\so101_right_leader.json
F:\260826_so101\huggingface\lerobot\calibration\robots\so_follower\so101_right_follower.json
```

不要无故重新校准，也不要对正常工作的机械臂运行 `lerobot-setup-motors`；它可能修改舵机 ID、波特率和 EEPROM 配置。

## 9. 摄像头与数据接口基准

ACT v1 输入：

```text
observation.state
observation.images.overhead
observation.images.right_wrist
```

输出：`action`。

| 项目 | 当前基准 |
|---|---|
| 顶视相机 key | `overhead` |
| 腕部相机 key | `right_wrist` |
| 分辨率 | 640×480 |
| FPS | 30 |
| `observation.state` | 6D |
| `action` | 6D |

历史测试使用过 `top` 和 `wrist`，后续采集与部署不得继续使用这些旧 key。OpenCV 相机编号可能随 USB 插拔变化，每次操作前都要重新识别实际 index；相机 key 不得随编号改变。

## 10. 数据集基准与本地盘点

ACT v1 实际训练数据：

| 项目 | 实际值 |
|---|---:|
| Episodes | 30 |
| Frames | 12,660 |
| FPS | 30 |
| Tasks | 1 |
| 任务文本 | `Pick up the blue box.` |
| State / Action | 6D / 6D |
| 相机 | `overhead`、`right_wrist` |
| 视频 | AV1、yuv420p、30 FPS |

云端训练命令使用的数据集标识：

```text
meyumei/so101-right-pick-blue-box-v1-70ep
```

其中 `70ep` 是名称的一部分，不代表训练实际读取了 70 条；训练报告和元信息均为 30 episodes。

本地已验证目录：

| 目录 | `meta/info.json` 实际状态 |
|---|---|
| `F:\260826_so101\datasets\so101-right-pick-blue-box-v1-30ep` | 30 episodes，12,660 frames |
| `F:\260826_so101\datasets\8有用so101-right-pick-blue-box-v1-70ep` | 10 episodes，4,388 frames |
| `F:\260826_so101\datasets\so101-right-move-blue-box-v1` | 存在，未确认为 ACT v1 训练输入 |
| `F:\260826_so101\datasets\so101-right-move-blue-box-v1-pilot-01` | 存在，pilot 数据 |

报告名称对应的精确本地路径 `F:\260826_so101\datasets\so101-right-pick-blue-box-v1-70ep` 当前不存在。不得根据目录名推断 episode 数量，始终以 `meta/info.json` 的 `total_episodes` 为准；本文件也不宣称现有数据目录可以直接相加或拼接。

## 11. ACT v1 云端训练基准

### 11.1 训练环境

| 组件 | 版本 |
|---|---|
| GPU | RTX 4090 |
| Python | 3.12.14 |
| LeRobot | 0.6.2 |
| Commit | `4aaff99be4a1d81568c08c8f0296b41b40c99ec4` |
| PyTorch / TorchVision | 2.10.0+cu128 / 0.25.0+cu128 |
| CUDA runtime | 12.8 |
| TorchCodec / PyAV / FFmpeg | 0.10.0 / 15.1.0 / 7.1.1 |

### 11.2 ACT 配置与结果

| 项目 | 实际值 |
|---|---:|
| Policy | ACT |
| Vision backbone | ResNet18，ImageNet pretrained |
| `dim_model` / `dim_feedforward` | 512 / 3200 |
| Attention heads | 8 |
| Encoder / Decoder layers | 4 / 1 |
| VAE encoder layers / latent dim | 4 / 32 |
| Chunk size / `n_action_steps` | 100 / 100 |
| Batch size | 8 |
| Steps | 10,000 |
| 近似 steps / epoch | 1,583 |
| 近似 epochs | 6.32 |
| 训练时间 | 约 13 分 15 秒 |
| 后期 loss | 约 0.19～0.23 波动 |

100-step smoke test 和 10,000-step 正式训练均已成功。仅凭 training loss 不能确定最佳模型，仍需真机比较。

```text
checkpoints：002000、004000、006000、008000、010000
```

## 12. 模型与部署基准

本地模型目录：

```text
F:\260826_so101\models\act_pick_blue_box_export
```

部署比较候选为 `006000`、`008000`、`010000`，第一测试候选：

```text
F:\260826_so101\models\act_pick_blue_box_export\008000
```

008000 已确认包含：

```text
config.json
model.safetensors
policy_preprocessor.json
policy_preprocessor_step_3_normalizer_processor.safetensors
policy_postprocessor.json
policy_postprocessor_step_0_unnormalizer_processor.safetensors
train_config.json
```

正式 rollout 前仍需确认：

- 当前右 Follower 实际 COM；
- `overhead` 和 `right_wrist` 的实际 OpenCV index；
- 模型能否在本地 cu126 环境成功加载并推理；
- 相机位置、画面方向和训练时一致；
- 机械臂周围已清空，人位于电源或急停附近。

首次自主运行使用 008000、5～10 秒短时测试，并准备立即停止。之后在固定场景比较 006000、008000、010000 的成功率。

## 13. 后续数据采集统一规则

1. 激活当前环境 `lerobot-so101-clean`，不要误用 backup。
2. 确认导入的是 F 盘固定 commit 的 LeRobot。
3. 每次重新确认 COM 和相机 index。
4. 相机 key 固定为 `overhead`、`right_wrist`。
5. 保持 640×480、30 FPS、6D state/action 和相同关节顺序。
6. 任务文本固定为 `Pick up the blue box.`。
7. 关闭 OBS 等占用相机的软件，固定相机位置、曝光、白平衡和焦距。
8. 正式批次前先录 1～3 条 smoke episodes，并检查视频、动作和实际 FPS。
9. 新批次优先写入新的 F 盘版本目录，不覆盖旧数据。
10. 若必须追加旧数据集，先备份并使用 `--resume=true`；所有 feature 必须完全一致。
11. 失败、掉帧、严重遮挡或未完成抓取的 episode 应重录。

LeRobot 0.6.2 相比历史 0.6.1 增加了更清楚的 episode/运行周期统计，并对完成后的数据集写入状态检查更严格；采集命令的主要参数没有变化。

## 14. 环境复核命令

```powershell
conda activate lerobot-so101-clean

python -c "import sys,lerobot,torch,torchvision,torchcodec,av; print(sys.version); print(lerobot.__version__, lerobot.__file__); print(torch.__version__, torch.version.cuda, torch.cuda.is_available()); print(torchvision.__version__); print(torchcodec.__version__); print(av.__version__)"

python -m pip check
& F:\miniconda3\envs\lerobot-so101-clean\Library\bin\ffmpeg.exe -version
git -C F:\260826_so101\lerobot-0.6.2 rev-parse HEAD

conda list
python -m pip freeze
conda env export --no-builds
```

## 15. 环境升级记录与边界

已执行：

1. 保留回退环境 `F:\miniconda3\envs\lerobot-so101-clean-backup`。
2. 在 `F:\260826_so101\lerobot-0.6.2` 获取官方源码并固定 commit。
3. 将当前环境中的 LeRobot 0.6.1 替换为源码 editable install 的 0.6.2。
4. 将 FFmpeg 8.1.2 调整为 7.1.1。
5. 执行 `pip check`，确认无破损依赖。

未执行：

- NVIDIA 驱动升级；
- PyTorch cu126 → cu128；
- 本地 ACT 模型加载；
- 本轮 Follower 或摄像头连接；
- 真机 rollout；
- 6k / 8k / 10k 真机比较。

## 16. 事实记录原则

- “当前本地环境”只指第 6 节的 0.6.2 Windows 工作环境。
- “备份环境”只指第 7 节的 0.6.1 回退环境。
- “云端训练环境”只指第 11 节的 cu128 环境。
- COM 和相机 index 是每次连接时的运行参数，不是永久事实。
- 数据集名称、目录名、episode 数量是三个不同概念；数量以元数据为准。
- 未实际完成的模型加载、相机连接和 rollout 不写成“已验证”。
- 环境或数据发生变化后，先更新本文件，再在 `09_实验记录.md` 记录对应实验。
