# SO-101 ACT 项目总览与环境基准

> 基准日期：2026-08-30  
> 项目根目录：`F:\260826_so101`  
> 本文件是项目当前状态与环境的统一入口。  
> 本版已加入 2026-08-30 ACT v1 真机部署与评估结果。

## 1. 项目目标

使用 Windows 本机连接 SO-101 Leader/Follower 和两路摄像头，完成右侧单臂视觉抓取的完整模仿学习流程：

```text
机械臂与相机准备
→ Leader 遥操作 Follower
→ 采集同步示范数据
→ 检查和整理数据
→ 云端训练 ACT
→ 导出 checkpoint
→ Windows 本机加载模型
→ 真机部署
→ 真机评估
→ 根据失败模式更新数据
```

当前任务：

```text
右侧 SO-101 Follower 根据顶视和腕部相机画面抓取蓝色盒子。
数据集任务文本：Pick up the blue box.
```

当前不是双臂同步训练任务。现阶段使用右侧一套 Leader/Follower 完成单臂采集、训练和部署；左侧机械臂保留供后续实验使用。

## 2. 文档导航

| 文档 | 作用 |
|---|---|
| [00_项目总览.md](./00_项目总览.md) | 当前项目事实、环境基准、进度和统一约束 |
| [01_硬件与环境.md](./01_硬件与环境.md) | 硬件概念、备份环境和云端环境记录 |
| [02_机械臂配置与校准.md](./02_机械臂配置与校准.md) | COM、设备 ID、舵机和校准说明 |
| [03_遥操作与摄像头.md](./03_遥操作与摄像头.md) | 遥操作流程与相机配置 |
| [04_数据采集.md](./04_数据采集.md) | Smoke test 和正式采集说明 |
| [05_数据检查与整理.md](./05_数据检查与整理.md) | 数据结构、视频和加载检查 |
| [06_ACT训练.md](./06_ACT训练.md) | 云端 ACT 训练命令与结果 |
| [07_模型导出与备份.md](./07_模型导出与备份.md) | Checkpoint 导出、传输和校验 |
| [08_真机部署.md](./08_真机部署.md) | 已验证的真机部署命令、参数与故障处理 |
| [09_实验记录.md](./09_实验记录.md) | ACT v1 真机实验结果 |
| [10_ACT_v1_完整实验报告与v2数据计划.md](./10_ACT_v1_完整实验报告与v2数据计划.md) | 第一版完整结论与第二版数据计划 |

### 文档时效说明

- `01_硬件与环境.md` 中 LeRobot 0.6.1 / FFmpeg 8.1.2 为历史采集和回退环境，不是当前本地工作环境。
- 当前本地工作环境：LeRobot 0.6.2、commit `4aaff99be4a1d81568c08c8f0296b41b40c99ec4`、FFmpeg 7.1.1。
- 历史相机 key `top`、`wrist` 已废弃；当前正式 key 固定为 `overhead`、`right_wrist`。
- 云端训练使用 cu128；Windows 本地部署使用 cu126，已实机验证可正常加载并推理。

## 3. 当前进度与结论

已完成：

- Windows 本地基础环境搭建和升级；
- 四台机械臂端口与固定 ID 记录；
- 右侧 Leader/Follower 校准确认；
- 右臂遥操作和两路相机验证；
- ACT v1 数据采集与数据检查；
- 云端 100-step smoke test；
- 云端 ACT 10,000-step 正式训练；
- 6k、8k、10k checkpoint 导出并下载到 Windows；
- Windows 本地 ACT 模型只读加载验证；
- 右 Follower + 两路相机 + CUDA 真机 rollout 链路验证；
- 008000 多次 25 秒真机评估；
- 010000 真机评估；
- `n_action_steps=25` 对照实验。

当前阶段结论：

```text
部署链路：成功
视觉到大范围接近动作：已学到
精细夹爪对准：未学稳
完整抓取：当前未成功
主要瓶颈：ACT v1 示范数据量与精细抓取一致性不足
```

当前暂停点已从“部署验证”转为：

```text
设计并采集 ACT v2 高质量数据
→ 重点加强最后 5~10 cm 的对准、夹取和抬升
→ 数据检查
→ 重新训练
→ 新一轮真机比较
```

## 4. 三套环境的身份与用途

| 环境 | 身份 | 用途 | 是否当前使用 |
|---|---|---|---|
| `lerobot-so101-clean` | Windows 当前工作环境 | 采集、模型加载、真机部署 | 是 |
| `lerobot-so101-clean-backup` | 升级前回退环境 | 追溯历史 0.6.1 流程 | 否 |
| `lerobot-so101-train` | 云端训练环境 | ACT 训练与 checkpoint 生成 | 云端使用 |

## 5. Windows 本机硬件基准

| 项目 | 实际值 |
|---|---|
| 操作系统 | Windows 11 家庭版 中文版 |
| 计算机 | ASUS TUF Gaming A15 FA507NV_FA507NV |
| CPU | AMD Ryzen 7 7735H |
| 内存 | 约 15.25 GiB |
| GPU | NVIDIA GeForce RTX 4060 Laptop GPU |
| GPU 显存 | 8,188 MiB |
| NVIDIA 驱动 | 555.97 |

已验证：

```text
torch.cuda.is_available() = True
GPU = NVIDIA GeForce RTX 4060 Laptop GPU
```

## 6. 当前 Windows 工作环境

| 组件 | 当前本地值 | 云端训练值 |
|---|---:|---:|
| Python | 3.12.14 | 3.12.14 |
| LeRobot | 0.6.2 | 0.6.2 |
| Commit | `4aaff99...` | `4aaff99...` |
| PyTorch | 2.10.0+cu126 | 2.10.0+cu128 |
| TorchVision | 0.25.0+cu126 | 0.25.0+cu128 |
| CUDA runtime | 12.6 | 12.8 |
| TorchCodec | 0.10.0 | 0.10.0 |
| PyAV | 15.1.0 | 15.1.0 |
| FFmpeg | 7.1.1 | 7.1.1 |

本地模型已在 cu126 环境成功加载并完成真机推理，因此当前不需要为了部署强行升级到 cu128。

## 7. 机械臂、端口与校准

| 设备 | 固定 ID | 2026-08-30 已验证 COM |
|---|---|---|
| 右 Leader | `so101_right_leader` | COM7 |
| 右 Follower | `so101_right_follower` | COM3 |
| 左 Leader | `so101_left_leader` | 历史 COM13 |
| 左 Follower | `so101_left_follower` | 历史 COM4 |

> COM 口可能因 USB 重新枚举而变化。每次使用前运行 `lerobot-find-port` 重新确认；固定 `robot.id` 才是 calibration 身份。

关节顺序：

```text
1 shoulder_pan
2 shoulder_lift
3 elbow_flex
4 wrist_flex
5 wrist_roll
6 gripper
```

不要无故重新校准，也不要对正常工作的机械臂重复执行 `lerobot-setup-motors`。

## 8. 摄像头与模型输入基准

ACT v1 模型实际输入：

```text
observation.state                 shape (6,)
observation.images.overhead       shape (3, 480, 640)
observation.images.right_wrist    shape (3, 480, 640)
```

输出：

```text
action                            shape (6,)
```

2026-08-30 当前部署会话中：

```text
overhead     = OpenCV Camera 1
right_wrist  = OpenCV Camera 2
```

统一要求：

```text
640 x 480
30 FPS
overhead
right_wrist
```

相机 index 可能因 USB 插拔改变；key 不得改变。

## 9. ACT v1 数据集基准

| 项目 | 实际值 |
|---|---:|
| Episodes | 30 |
| Frames | 12,660 |
| FPS | 30 |
| Tasks | 1 |
| 任务文本 | `Pick up the blue box.` |
| State / Action | 6D / 6D |
| Cameras | `overhead`、`right_wrist` |
| 视频 | AV1、yuv420p、30 FPS |

训练使用 repo_id：

```text
meyumei/so101-right-pick-blue-box-v1-70ep
```

注意：`70ep` 只是名称的一部分，ACT v1 实际训练读取的是 30 episodes。

## 10. ACT v1 训练基准

| 项目 | 实际值 |
|---|---:|
| Policy | ACT |
| Vision backbone | ResNet18 |
| Chunk size | 100 |
| `n_action_steps` | 100 |
| Batch size | 8 |
| Steps | 10,000 |
| 近似 steps / epoch | 1,583 |
| 近似 epochs | 6.32 |
| 训练 GPU | RTX 4090 |
| 训练时间 | 约 13 分 15 秒 |
| 后期 loss | 约 0.19～0.23 |

checkpoints：

```text
002000
004000
006000
008000
010000
```

## 11. 模型导出与本地加载

本地目录：

```text
F:\260826_so101\models\act_pick_blue_box_export
├── 006000
├── 008000
└── 010000
```

008000 已确认可通过：

```cmd
python -c "from lerobot.policies.act import ACTPolicy; p=ACTPolicy.from_pretrained(r'F:\260826_so101\models\act_pick_blue_box_export\008000'); print('MODEL LOAD OK'); print(p.config)"
```

成功加载，使用 CUDA 推理。

## 12. 当前已验证真机部署参数

2026-08-30 当前会话：

```text
Follower COM       = COM3
robot.id           = so101_right_follower
overhead index     = 1
right_wrist index  = 2
CUDA               = True
GPU                = RTX 4060 Laptop GPU
```

008000 默认配置的稳定 rollout 命令：

```cmd
lerobot-rollout --strategy.type=base --policy.path="F:\260826_so101\models\act_pick_blue_box_export\008000" --robot.type=so101_follower --robot.port=COM3 --robot.id=so101_right_follower --robot.cameras="{overhead: {type: opencv, index_or_path: 1, width: 640, height: 480, fps: 30}, right_wrist: {type: opencv, index_or_path: 2, width: 640, height: 480, fps: 30}}" --task="Pick up the blue box." --fps=30 --duration=25 --device=cuda --return_to_initial_position=True
```

> 当前 ACT 模型没有语言输入 feature，因此 `--task` 不决定 ACT 的视觉动作；但为保证元数据一致，后续统一使用数据集中的 `Pick up the blue box.`。

## 13. 真机实验结论

### 13.1 008000

- 5 秒 smoke rollout：机械臂成功自主运动，部署链路打通。
- 15 秒探索测试：会向蓝色方块靠近，但未成功抓取。
- 25 秒系统测试：在采集时的多个典型物块位置重复测试 4 次。
- 结果：4/4 都会向物块靠近；0/4 完整抓取成功；夹爪没有稳定进入物块两侧，靠近后常在物块附近徘徊。

结论：

```text
reaching：已学到
fine alignment：不足
grasping：未学稳
```

### 13.2 010000

默认 `n_action_steps=100` 真机测试仍表现为：

```text
会靠近物块
但没有形成正确夹取
```

相比 008000 未观察到解决精细抓取问题的明显改善。

### 13.3 `n_action_steps=25` 对照实验

对 008000 使用：

```text
--policy.n_action_steps=25
```

表现反而变差，未形成原先稳定的靠近轨迹，同时更频繁触发完整 ACT 推理，运行日志出现持续约 12~14 Hz 的慢循环警告。

因此当前模型部署恢复并保持训练配置：

```text
n_action_steps = 100
```

不建议继续使用 25。

## 14. 相机故障记录

曾有一次 25 秒 rollout 在约 3.8 秒提前结束：

```text
TimeoutError: OpenCVCamera(2) latest frame is too old:
523.8 ms (max allowed: 500 ms)
```

判断：`right_wrist` Camera 2 暂时停止更新超过 LeRobot 500 ms 门限，属于相机/USB 帧新鲜度故障，不是 ACT 模型报错。

之后连续多次 25 秒测试完整结束，说明该问题目前属于偶发事件，但后续仍应：

- 关闭 OBS / Windows Camera 等占用摄像头的软件；
- 固定腕部相机 USB 线，避免运动中拉扯；
- 尽量让两路相机使用稳定 USB 接口；
- 若再次频繁出现，可测试 `fourcc: MJPG` 以降低 USB 带宽压力。

## 15. 当前阶段判断

综合 008000 多位置评估和 010000 结果，当前优先判断为：

```text
训练和部署流程本身已成功
↓
模型能够建立“图像 → 接近物体”的映射
↓
但最后几厘米的夹爪姿态、对准、闭合时机没有稳定学会
↓
继续单纯增加训练 step 的收益预计有限
↓
下一步优先提高示范数据的数量、质量和抓取阶段一致性
```

## 16. ACT v2 数据采集统一规则

保持不变：

```text
robot.id = so101_right_follower
teleop.id = so101_right_leader
camera keys = overhead, right_wrist
resolution = 640 x 480
fps = 30
state/action = 6D
任务 = Pick up the blue box.
```

采集重点从“更多轨迹”转为“更稳定的抓取示范”：

1. Approach 方向尽量一致。
2. 最后 5~10 cm 明显减速并完成视觉对准。
3. 保持 wrist_roll 抓取方向尽量一致。
4. 夹爪中心线对准物块中心。
5. 夹爪两指明确位于物块两侧后再闭合。
6. 闭合后保持短暂稳定，再抬升。
7. 失败、碰撞、明显偏夹、掉帧、严重遮挡 episode 重录。
8. 保留一定物块位置变化，但不要让同一物块位置对应多种完全不同的抓法。
9. 正式追加前先录少量 smoke episodes，并逐条观看最后抓取阶段。
10. 新版本使用新目录，不覆盖 ACT v1 数据。

## 17. 下一阶段

```text
设计 ACT v2 数据规范
→ 采集高质量新增示范
→ 检查最后 5~10 cm 的抓取一致性
→ 训练 ACT v2
→ 固定场景评估
→ 再测试位置泛化
```

当前项目阶段已经从“能否部署”进入“如何提高策略成功率”。
