# 机械臂配置与校准

## 1. 查找串口

使用：

```cmd
lerobot-find-port
```

当前历史记录：

```text
右 Leader    COM7
左 Leader    COM13
右 Follower  COM3
左 Follower  COM4
```

> COM 口可能因重新插拔、USB 转串口枚举等变化。每次使用前应重新确认，不要永久假设编号固定。

---

## 2. 舵机 ID / 波特率

⚠️ **只在机械臂首次装配、舵机更换或 EEPROM 配置确实需要修改时设置。**

已有正常工作的机械臂不要随意重复运行电机设置流程，因为可能修改：

- 舵机 ID
- 波特率
- EEPROM 中的底层配置

---

## 3. SO101 关节名称与舵机顺序

```text
ID 1  shoulder_pan
ID 2  shoulder_lift
ID 3  elbow_flex
ID 4  wrist_flex
ID 5  wrist_roll
ID 6  gripper
```

---

## 4. 校准原则

校准用于建立机械臂的运动范围与归一化映射。

同一台机械臂应持续使用相同的：

```text
robot.id
teleop.id
```

这样 LeRobot 才能找到对应 calibration。

已有正确 calibration 时，不要无故重新校准。

---

## 5. Leader 校准示例

```cmd
lerobot-calibrate ^
  --teleop.type=so101_leader ^
  --teleop.port=COM13 ^
  --teleop.id=so101_left_leader
```

历史笔记中的流程：

1. 先将机械臂移动到中间姿态。
2. 根据交互提示进行校准。
3. 移动各关节寻找所需运动范围/限位。

---

## 6. Follower 校准

当前原始笔记未记录一条已经确认执行过的完整 Follower 校准命令。

后续如需要重新校准，应在确认官方当前 CLI 参数后补充到这里。

---

## 7. 只读检查右 Follower 舵机 ID / 位置

历史检查命令：

```cmd
python -c "from lerobot.motors import Motor,MotorNormMode; from lerobot.motors.feetech import FeetechMotorsBus; names=['shoulder_pan','shoulder_lift','elbow_flex','wrist_flex','wrist_roll','gripper']; motors={n:Motor(i,'sts3215',MotorNormMode.DEGREES) for i,n in enumerate(names,1)}; b=FeetechMotorsBus('COM3',motors); b.connect(); print({n:b.read('Present_Position',n,normalize=False) for n in names}); b.disconnect(disable_torque=False)"
```
