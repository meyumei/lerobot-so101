# 数据采集

## 1. 采集前顺序

```text
识别端口
→ 确认 calibration
→ 遥操作验证
→ 接入摄像头
→ 本地小样本录制
→ 检查数据
→ 正式采集
```

---

## 2. 历史 1 episode 相机测试命令

```cmd
lerobot-record 
--robot.type=so101_follower 
--robot.port=COM3 
--robot.id=so101_right_follower 
--robot.cameras="{overhead: {type: opencv, index_or_path: 1, width: 640, height: 480, fps: 30}, right_wrist: {type: opencv, index_or_path: 2, width: 640, height: 480, fps: 30}}" 
--teleop.type=so101_leader 
--teleop.port=COM7 
--teleop.id=so101_right_leader 
--dataset.repo_id=meyumei/so101-right-pick-blue-box-v1-70ep 
--dataset.root="F:\260826_so101\datasets\so101-right-pick-blue-box-v1-30ep" 
--dataset.fps=30 
--dataset.num_episodes=2 
--dataset.episode_time_s=15 
--dataset.reset_time_s=20 
--dataset.single_task="Pick up the blue box." 
--dataset.push_to_hub=false 
--dataset.streaming_encoding=true 
--dataset.encoder_threads=2 
--resume=true
```

> 这是历史测试命令。当前正式流程建议把相机 key 统一改为 `overhead` 和 `right_wrist`。

直接复制版本

lerobot-record --robot.type=so101_follower --robot.port=COM3 --robot.id=so101_right_follower --robot.cameras="{overhead: {type: opencv, index_or_path: 1, width: 640, height: 480, fps: 30}, right_wrist: {type: opencv, index_or_path: 2, width: 640, height: 480, fps: 30}}" --teleop.type=so101_leader --teleop.port=COM7 --teleop.id=so101_right_leader --dataset.repo_id=meyumei/so101-right-pick-blue-box-v1-70ep --dataset.root="F:\260826_so101\datasets\so101-right-pick-blue-box-v1-30ep" --dataset.fps=30 --dataset.num_episodes=2 --dataset.episode_time_s=15 --dataset.reset_time_s=15 --dataset.single_task="Pick up the blue box." --dataset.push_to_hub=false --dataset.streaming_encoding=true --dataset.encoder_threads=2 --resume=true

---

## 3. 左侧 smoke 采集模板

```powershell
lerobot-record --robot.type=so101_follower --robot.port=COMx --robot.id=so101_left_follower --teleop.type=so101_leader --teleop.port=COMy --teleop.id=so101_left_leader --robot.cameras="{ front: {type: opencv, index_or_path: 0, width: 640, height: 480, fps: 30}}" --dataset.repo_id=你的用户名/so101-left-smoke --dataset.root=F:\260826_so101\huggingface\lerobot\datasets\so101-left-smoke --dataset.single_task="将物体从起点移动到目标位置" --dataset.num_episodes=2 --dataset.episode_time_s=15 --dataset.reset_time_s=15 --dataset.push_to_hub=False
```

说明：

- Follower 状态、Leader 动作、摄像头视频同步写入 LeRobotDataset。
- Smoke test 先录少量 episode。
- `push_to_hub=False` 时只保存在本地。

---

## 4. 当前项目正式训练数据

当前 ACT v1 使用的数据：

```text
实际 Episodes: 30
Frames:        12660
FPS:           30
```

相机：

```text
observation.images.overhead
observation.images.right_wrist
```

机器人状态：

```text
observation.state: 6D
```

动作：

```text
action: 6D
```

6 个关节：

```text
shoulder_pan.pos
shoulder_lift.pos
elbow_flex.pos
wrist_flex.pos
wrist_roll.pos
gripper.pos
```

---

## 5. 正式采集建议

正式采集时：

- 保持相机位置稳定。
- 保持任务定义一致。
- 保持 robot.id / teleop.id 一致。
- 尽量覆盖一定范围的初始物体位置。
- 采集后先检查数据，再开始训练。
